const btn_menu = document.getElementById('btn_menu');
const showMenu = document.getElementById('showMenu');
const close__menu = document.querySelector('.btn_close');
const btns_cards = document.querySelectorAll('#btn_card');
const modal_cards = document.querySelector('.modal_cards');
const container_cards = document.querySelector('.container_cards');
const close_cards = document.querySelectorAll('.btn_close');
const slide_product = document.querySelector('.slide_product');
const list = document.querySelector('.list');
const btn_next_product = document.querySelectorAll('.btn_next_product');
const btn_pre_product = document.querySelectorAll('.btn_pre_product');
const firstSlideWidth = list.querySelector('.slide').offsetWidth;
const listChildrens = [...list.children];
